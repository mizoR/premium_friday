require "spec_helper"

RSpec.describe PremiumFriday do
  it "has a version number" do
    expect(PremiumFriday::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
